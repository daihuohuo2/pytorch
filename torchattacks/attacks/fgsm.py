"""FGSM 攻击复盘占位文件。

FGSM 适合作为第一个白盒攻击来复盘，因为它只用一步梯度就能生成对抗样本。

核心公式：

    adv_x = x + eps * sign(grad_x loss(model(x), y))

含义：

- x 是原始图片；
- y 是真实标签；
- eps 是允许扰动的最大幅度；
- model(x) 是模型对原图的预测；
- loss(model(x), y) 是分类损失；
- grad_x 表示损失函数对输入图片 x 的梯度；
- sign(...) 表示只取梯度方向，不取具体大小。
"""
